{
  "name": "pourquoi-factory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "p1:validate": "npm run build && node dist/pipeline/text/cli.js episodes/fixtures/p1-valid.json",
    "p1:pilot": "npm run build && node dist/pipeline/text/cli.js episodes/pilot-ciel-bleu/text-bundle.json"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  }
}
